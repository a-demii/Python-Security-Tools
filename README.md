🛡️ Project 1: Basic Port Scanner
This is a simple Python tool I built to understand the fundamentals of network sockets.

How it works:

Uses the socket library to attempt a TCP connection.

Reports whether a specific port is open or closed.

Implements a timeout to ensure efficient scanning.
